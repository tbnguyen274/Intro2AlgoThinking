# Bài 17: Tìm Số Nhỏ Nhất Chỉ Chứa 0,1 Chia Hết Cho N

## Đề Bài

Cho số nguyên dương **n**. Hãy tìm số nguyên dương **m** nhỏ nhất có không quá 9 chữ số thỏa mãn:
- **m** chỉ chứa các chữ số {0, 1}
- **m** là bội của **n** (m chia hết cho n)

**Dữ liệu vào:**
- Gồm một số nguyên dương $n$ ($n \leq 100$)

**Kết quả:**
- Gồm một số nguyên duy nhất là **m**

**Ví dụ:**
```
INPUT:
6

OUTPUT:
1110
```

---

## Phân Tích Bài Toán

### Hiểu Đề Bài

Tìm số nhỏ nhất chỉ gồm chữ số 0 và 1 mà chia hết cho n.

**Ví dụ:** n = 6

Các số chỉ chứa 0,1 theo thứ tự tăng dần:
```
1    % 6 = 1  ✗
10   % 6 = 4  ✗
11   % 6 = 5  ✗
100  % 6 = 4  ✗
101  % 6 = 5  ✗
110  % 6 = 2  ✗
111  % 6 = 3  ✗
1000 % 6 = 4  ✗
1001 % 6 = 5  ✗
1010 % 6 = 2  ✗
1011 % 6 = 3  ✗
1100 % 6 = 2  ✗
1101 % 6 = 3  ✗
1110 % 6 = 0  ✓
```

**Output:** 1110

### Tại Sao Luôn Có Đáp Án?

**Nguyên lý Dirichlet:**

Xét dãy: 1, 11, 111, 1111, 11111, ..., 111...1 (n+1 số 1)

- Có **n+1** số
- Mỗi số có phần dư khi chia cho n: {0, 1, 2, ..., n-1} (n giá trị)
- Theo Pigeonhole: **ít nhất 2 số có cùng phần dư** hoặc **có 1 số có phần dư = 0**

**Trường hợp 1:** Có số có phần dư = 0 → Đáp án!

**Trường hợp 2:** Có 2 số a > b với $a \equiv b \pmod{n}$
- $(a - b) \equiv 0 \pmod{n}$
- $a - b$ có dạng 111...1000...0 (chỉ chứa 0,1)
- Đáp án!

→ **Luôn tồn tại đáp án!**

---

## Approach 1: Brute Force - Sinh Tất Cả

### Ý Tưởng

1. **Sinh tất cả số** chỉ chứa 0,1 theo thứ tự tăng dần
2. **Kiểm tra từng số** xem có chia hết cho n không
3. **Trả về số đầu tiên** thỏa mãn

### Cách Sinh Số

**Quan sát:** Số chỉ chứa 0,1 giống như số nhị phân!

```
Binary → Decimal (chỉ 0,1)
1      → 1
10     → 10
11     → 11
100    → 100
101    → 101
110    → 110
111    → 111
...
```

Duyệt i từ 1 đến $2^9 - 1$, chuyển i sang binary, xem như số thập phân.

### Code Implementation

```cpp
long long binaryToDecimal01(int num) {
    long long result = 0;
    long long multiplier = 1;
    
    while (num > 0) {
        int bit = num % 2;
        result += bit * multiplier;
        multiplier *= 10;
        num /= 2;
    }
    
    return result;
}

string bruteForceSolution(int n) {
    for (int i = 1; i < (1 << 9); i++) { // 2^9 = 512
        long long num = binaryToDecimal01(i);
        if (num % n == 0) {
            return to_string(num);
        }
    }
    return "-1";
}
```

### Phân Tích Độ Phức Tạp

**Time Complexity:** $O(2^k \times k)$

- k = 9 (số chữ số tối đa)
- $2^9 = 512$ số cần kiểm tra
- Mỗi số: chuyển đổi và kiểm tra $O(k)$

**Space Complexity:** $O(1)$

### Đánh Giá

Với k = 9:
- $O(512 \times 9) \approx 4600$ operations
- **Chấp nhận được!**

**Vấn đề:**
- Số có thể rất lớn (111111111 ≈ $10^9$)
- Cần dùng `long long` để tránh overflow

---

## Approach 2: BFS - Tối Ưu

### Ý Tưởng Chính

**Key Insight:** Thay vì lưu số m, chỉ lưu **phần dư** $m \bmod n$!

**Tại sao?**
- Chỉ quan tâm m có chia hết cho n hay không
- Phần dư $\in \{0, 1, 2, \ldots, n-1\}$ → Tối đa **n trạng thái**
- Dirichlet: Chắc chắn gặp phần dư = 0 trong n bước!

### BFS State

**State:** `(remainder, number_string)`
- `remainder`: $m \bmod n$
- `number_string`: chuỗi biểu diễn số m

**Start State:** $(1 \bmod n, \text{"1"})$

**Transition:**
- Từ state $(r, s)$, sinh 2 state mới:
  - Thêm '0': $((r \times 10) \bmod n, s + \text{"0"})$
  - Thêm '1': $((r \times 10 + 1) \bmod n, s + \text{"1"})$

**Goal State:** $remainder = 0$

**Visited:** Đánh dấu các remainder đã thăm để tránh lặp

### Visualization

**Example:** n = 6

```
Level 0:  (1, "1")
          ↙     ↘
Level 1: (4, "10")  (5, "11")
          ↙   ↘      ↙    ↘
Level 2: (4*) (5*)  (2, "110") (3, "111")
                     ↙   ↘        ↙   ↘
Level 3:          (2*) (0, "1110") (3*) (1*)

* = already visited
```

**Tìm thấy:** $(0, \text{"1110"})$ → Output: 1110

### Code Implementation

**Tóm tắt:**
Hàm dùng **BFS trên phần dư modulo n** để tìm **số nhỏ nhất chỉ gồm 0 và 1** chia hết cho `n`: mỗi trạng thái là `(phần dư, chuỗi số)`, mỗi bước thêm `0` hoặc `1`.
Khi gặp phần dư `0` thì dừng, dùng `visited` để tránh lặp và giới hạn độ dài để kiểm soát không gian tìm kiếm.


```cpp
#include <iostream>
#include <queue>
#include <vector>
#include <string>
using namespace std;

string bfsSolution(int n) {
    if (n == 1) return "1";
    
    queue<pair<int, string>> q;
    vector<bool> visited(n, false);
    
    q.push({1 % n, "1"});
    visited[1 % n] = true;
    
    while (!q.empty()) {
        int remainder = q.front().first;
        string num_str = q.front().second;
        q.pop();
        
        // Kiểm tra nếu chia hết
        if (remainder == 0) {
            return num_str;
        }
        
        // Giới hạn độ dài (không quá 9 chữ số)
        if (num_str.length() >= 9) continue;
        
        // Thêm '0'
        int new_remainder_0 = (remainder * 10) % n;
        if (!visited[new_remainder_0]) {
            visited[new_remainder_0] = true;
            q.push({new_remainder_0, num_str + "0"});
        }
        
        // Thêm '1'
        int new_remainder_1 = (remainder * 10 + 1) % n;
        if (!visited[new_remainder_1]) {
            visited[new_remainder_1] = true;
            q.push({new_remainder_1, num_str + "1"});
        }
    }
    
    return "-1";
}
```

### Phân Tích Độ Phức Tạp

**Time Complexity:** $O(n)$

- Tối đa n trạng thái (n phần dư khác nhau)
- Mỗi trạng thái xử lý $O(1)$

**Space Complexity:** $O(n)$

- Queue: $O(n)$
- Visited array: $O(n)$

### Đánh Giá

Với $n \leq 100$:
- $O(100)$ operations
- **Rất nhanh! Tối ưu!**

**So sánh với Brute Force:**
- Brute Force: $O(2^9) = 512$ (độc lập với n)
- BFS: $O(n) \leq 100$
- BFS **nhanh hơn và đảm bảo hơn**!

---

## Simulation Chi Tiết

### Test Case: n = 6

**Initial:**
- Queue: `[(1, "1")]`
- Visited: `{1}`

**Iteration 1:**
- Pop: `(1, "1")`
- remainder = 1 ≠ 0
- Add '0': $(1 \times 10) \bmod 6 = 10 \bmod 6 = 4$ → `(4, "10")`
- Add '1': $(1 \times 10 + 1) \bmod 6 = 11 \bmod 6 = 5$ → `(5, "11")`
- Queue: `[(4, "10"), (5, "11")]`
- Visited: `{1, 4, 5}`

**Iteration 2:**
- Pop: `(4, "10")`
- remainder = 4 ≠ 0
- Add '0': $(4 \times 10) \bmod 6 = 40 \bmod 6 = 4$ (visited)
- Add '1': $(4 \times 10 + 1) \bmod 6 = 41 \bmod 6 = 5$ (visited)
- Queue: `[(5, "11")]`

**Iteration 3:**
- Pop: `(5, "11")`
- remainder = 5 ≠ 0
- Add '0': $(5 \times 10) \bmod 6 = 50 \bmod 6 = 2$ → `(2, "110")`
- Add '1': $(5 \times 10 + 1) \bmod 6 = 51 \bmod 6 = 3$ → `(3, "111")`
- Queue: `[(2, "110"), (3, "111")]`
- Visited: `{1, 2, 3, 4, 5}`

**Iteration 4:**
- Pop: `(2, "110")`
- remainder = 2 ≠ 0
- Add '0': $(2 \times 10) \bmod 6 = 20 \bmod 6 = 2$ (visited)
- Add '1': $(2 \times 10 + 1) \bmod 6 = 21 \bmod 6 = 3$ (visited)
- Queue: `[(3, "111")]`

**Iteration 5:**
- Pop: `(3, "111")`
- remainder = 3 ≠ 0
- Add '0': $(3 \times 10) \bmod 6 = 30 \bmod 6 = 0$ → `(0, "1110")` **Found!**

**Output:** `1110`

---

## Approach 3: Space Optimization

### Ý Tưởng

Thay vì lưu chuỗi trong queue (tốn space), lưu **parent pointers** để reconstruct.

**Data structures:**
- `parent[r]`: remainder trước đó dẫn đến r
- `digit[r]`: chữ số (0 hoặc 1) đã thêm để đến r

**Reconstruct:**
1. Bắt đầu từ `remainder = 0`
2. Trace ngược qua `parent[]`
3. Lưu các `digit[]` và reverse

### Code Implementation

```cpp
string bfsOptimized(int n) {
    if (n == 1) return "1";
    
    queue<int> q;
    vector<bool> visited(n, false);
    vector<int> parent(n, -1);
    vector<int> digit(n, -1);
    
    q.push(1 % n);
    visited[1 % n] = true;
    
    while (!q.empty()) {
        int remainder = q.front();
        q.pop();
        
        if (remainder == 0) {
            // Reconstruct
            string result = "";
            int current = 0;
            while (parent[current] != -1) {
                result += (char)('0' + digit[current]);
                current = parent[current];
            }
            result += "1";
            reverse(result.begin(), result.end());
            return result;
        }
        
        // Add '0'
        int new_r0 = (remainder * 10) % n;
        if (!visited[new_r0]) {
            visited[new_r0] = true;
            parent[new_r0] = remainder;
            digit[new_r0] = 0;
            q.push(new_r0);
        }
        
        // Add '1'
        int new_r1 = (remainder * 10 + 1) % n;
        if (!visited[new_r1]) {
            visited[new_r1] = true;
            parent[new_r1] = remainder;
            digit[new_r1] = 1;
            q.push(new_r1);
        }
    }
    
    return "-1";
}
```

**Space:** $O(n)$ nhưng tiết kiệm hơn (không lưu strings trong queue)

---

## Solution Cuối Cùng

```cpp
#include <iostream>
#include <queue>
#include <vector>
#include <string>
using namespace std;

string findSmallestMultiple(int n) {
    if (n == 1) return "1";
    
    queue<pair<int, string>> q;
    vector<bool> visited(n, false);
    
    q.push({1 % n, "1"});
    visited[1 % n] = true;
    
    while (!q.empty()) {
        int remainder = q.front().first;
        string num_str = q.front().second;
        q.pop();
        
        if (remainder == 0) {
            return num_str;
        }
        
        if (num_str.length() >= 9) continue;
        
        int new_r0 = (remainder * 10) % n;
        if (!visited[new_r0]) {
            visited[new_r0] = true;
            q.push({new_r0, num_str + "0"});
        }
        
        int new_r1 = (remainder * 10 + 1) % n;
        if (!visited[new_r1]) {
            visited[new_r1] = true;
            q.push({new_r1, num_str + "1"});
        }
    }
    
    return "-1";
}

int main() {
    int n;
    cin >> n;
    
    string result = findSmallestMultiple(n);
    cout << result << endl;
    
    return 0;
}
```

---

## Test Cases

### Test 1: Ví dụ đề bài
```
Input:
6

Output:
1110
```

### Test 2: n = 1
```
Input:
1

Output:
1
```
*Giải thích:* Mọi số đều chia hết cho 1

### Test 3: n = 2
```
Input:
2

Output:
10
```

### Test 4: n = 7
```
Input:
7

Output:
1001
```
*Giải thích:* 1001 = 7 × 143

### Test 5: n = 17
```
Input:
17

Output:
11101
```

---

## Tổng Kết Phỏng Vấn

### Quy Trình Giải Quyết

1. **Nhận dạng bài toán:** Number theory + Graph search
2. **Brute force:** Sinh tất cả số, kiểm tra từng cái
3. **Tối ưu với BFS:** Chỉ lưu phần dư, tìm kiếm theo graph
4. **Pigeonhole Principle:** Chứng minh luôn có đáp án
5. **Space optimization:** Parent pointers để reconstruct

### Key Points

- **Time:** $O(n)$ với BFS
- **Space:** $O(n)$
- **Pigeonhole Principle:** Đảm bảo tồn tại đáp án
- **Modular arithmetic:** Chỉ cần lưu phần dư, không cần số thật

### Câu Hỏi Mở Rộng Có Thể Gặp

**Q: Nếu cho phép chữ số {0, 1, 2}?**
- A: Tương tự BFS, mỗi state sinh 3 state mới. Complexity vẫn O(n).

**Q: Nếu n rất lớn (10^6)?**
- A: BFS vẫn O(n), nhưng space O(n) có thể lớn. Có thể dùng iterative deepening hoặc optimization khác.

**Q: Tại sao không dùng DFS?**
- A: BFS đảm bảo tìm số **nhỏ nhất** (level by level). DFS có thể tìm số dài hơn.

**Q: Nếu không giới hạn 9 chữ số?**
- A: Vẫn áp dụng BFS. Theo nguyên lý Dirichlet, chắc chắn tìm thấy trong n bước, nên số chữ số ≤ n.

**Q: Bài toán liên quan?**
- A: Modular arithmetic problems, BFS on implicit graphs, Number theory - divisibility
